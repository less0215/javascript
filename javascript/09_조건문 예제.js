
// 문제 1번 : 3의 배수면 '박수'라는 글자가 나올 수 있게끔 코드를 짜야 함.

function 삼육구게임(숫자) {
    if (숫자 % 3 === 0) {
    console.log('박수');
    } else {
    console.log('너 틀림');
    }
};

삼육구게임(3)

// 문제 2번 업그레이드 : 
// 삼육구게임(6); 실행하면 콘솔창에 '박수'라는 글자가 떠야합니다.
// 삼육구게임(9); 실행하면 콘솔창에 '박수x2'라는 글자가 떠야합니다.
// 삼육구게임(11); 실행하면 콘솔창에 '통과'라는 글자가 떠야합니다.

// 조건문에 else if를 넣는다면, 조건문 순서가 중요하다.
// 조건문 순서는 다음과 같은 점을 고려해서 정한다.
// 1. 조금 더 구체적인 조건을 상단에 위치
// 2. 예외상황이 있다면, 해당 상황에 부합하는 조건을 상단에 위치

// 2번 문제는 '9의 배수'가 '3의 배수'보다 더 구체적인 조건임.
// 예: 9 → 9의 배수이자 3의 배수
// 3, 6, 12, 15 → 3의 배수이지만 9의 배수는 아님


function 삼육구게임(숫자) {
    if (숫자 % 9 == 0) {
    console.log('박수x2');
    } else if (숫자 % 3 == 0) {
    console.log('박수');
    } else {
    console.log('통과');
    }
}

// 문제3번:
// 공인중개사 1차 시험은 개론, 민법 2개 과목이 있습니다.
// 과목마다 100점 만점이지만 두 과목 합해서 120점 이상이면 합격시켜줍니다. 
// 다만 한 과목이 40점 미만이면 과락으로 불합격됩니다.  
// 과목 점수 2개를 파라미터로 입력하면 합격인지 불합격인지 여부를 콘솔창에 출력하는 함수를 만들어보십시오. 

function 합격했냐(a, b){
    if ( a < 40 || b < 40 ) {
      console.log('불합격')
    } else if (a + b >= 120) {
      console.log('합격')
    } else {
      console.log('불합격')
    }
  }


  // (응용) 원래의 369게임 룰을 적용하려면 어떻게 해야할까요?
  // 3의 배수에서 박수를 치는게 아니라 끝자리가 3,6,9로 끝나는 숫자라면 '박수'를 출력되게 하는겁니다. 
  // 끝자리가 3, 6, 9이면 '박수'
  
  
  function 삼육구게임(숫자) {
      if(숫자 % 10 === 3 || 숫자 % 10 === 6 || 숫자 % 10 === 9) {
          console.log('박수');
      } else {
          console.log('통과');
      }
  }
  
  
  
  // (응용2) 합격판독기에 0에서 100사이 숫자가 아닌걸 입력하면 장난치지 말라고 alert를 띄우려면 어떻게 코드짜야할까요?
  // 이것도 간단하게 if문 알아서 추가해봅시다.
  
  
  function 합격판독기(입력) {
      if (typeof 입력 !== 'number') {
          alert('장난치지 마라');
      } else {  // ✅ 숫자가 아닌 경우를 이미 체크했으므로
          console.log('입력하신 숫자는: ' + 입력);
      }
  }